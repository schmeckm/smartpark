# Wartezeiten.APP adapter

Integrates [Wartezeiten.APP](https://wartezeiten.app) park and attraction wait times into Smart Park OS.

## Configuration

Optional **`parkId`** for scoped polling when supported by your deployment.

## Outputs

Observations use domain `rides` and metric `queue_time` (minutes).
