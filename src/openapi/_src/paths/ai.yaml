/ai/health:
  get:
    tags:
      - AI
    summary: 'AI module health'
    description: 'Liveness and sampling config (unauthenticated, no RBAC)'
    security: []
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeAiHealth'
/ai/pipeline-health:
  get:
    tags:
      - AI
    summary: 'Last AI full-pipeline run and scheduler config'
    description: 'Needs `ai.read`. Optional smoke hints from `data/ai-smoke-status.json` when present.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  additionalProperties: true
/ai/forecasts/refresh:
  post:
    tags:
      - AI
    summary: 'Sample zones and regenerate baseline crowd forecasts'
    description: 'Needs permission `ai.refresh`'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeAiRefresh'
/ai/forecasts:
  get:
    tags:
      - AI
    summary: 'List current forecasts'
    description: 'Optional filters; needs `ai.read`'
    parameters:
      -
        in: query
        name: horizonMinutes
        schema:
          type: integer
          minimum: 1
      -
        in: query
        name: subjectType
        schema:
          type: string
          enum:
            - PARK
            - ZONE
            - RIDE
      -
        in: query
        name: targetMetric
        schema:
          type: string
          enum:
            - CROWD_LEVEL
            - WAIT_TIME
            - STAFF_DEMAND
      -
        in: query
        name: limit
        schema:
          type: integer
          default: 100
          minimum: 1
          maximum: 200
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeForecasts'
/ai/insights/summary:
  get:
    tags:
      - AI
    summary: 'Top hotspot summary (60 min baseline)'
    description: 'Needs `ai.read`'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeAiInsights'
/ai/pipeline-runs:
  get:
    tags:
      - AI
    summary: 'Recent AI pipeline / ingestion runs (admin diagnostics)'
    description: 'Needs **`ai.read`**. Optional **`limit`** (1–100, default 25).'
    parameters:
      -
        in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 25
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/studio/catalog:
  get:
    tags:
      - AI
    summary: 'ML Studio catalog (features, targets, algorithms)'
    description: 'Needs **`ai.read`**.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/studio/dataset-stats:
  get:
    tags:
      - AI
    summary: 'ML Studio dataset statistics for an entity'
    description: 'Needs **`ai.read`**, **`X-Park-Id`**, and merged query validation (`entityType`, optional `entityId`, `dataset`).'
    parameters:
      -
        in: query
        name: entityType
        required: true
        schema:
          type: string
          description: 'e.g. RIDE'
          park_asset: 'per server allowlist'
      -
        in: query
        name: entityId
        schema:
          type: string
          format: uuid
      -
        in: query
        name: dataset
        schema:
          type: string
          enum:
            - FEATURE_STORE
            - SANDBOX
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
/ai/studio/models:
  get:
    tags:
      - AI
    summary: 'List ML Studio trained model rows (park-scoped)'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Supports filters `entityType`, `targetVariable`, `modelScope`, `activeOnly`, `limit`.'
    parameters:
      -
        in: query
        name: entityType
        schema:
          type: string
      -
        in: query
        name: targetVariable
        schema:
          type: string
      -
        in: query
        name: modelScope
        schema:
          type: string
          enum:
            - entity
            - category
            - park
      -
        in: query
        name: activeOnly
        schema:
          type: string
          enum:
            - 'true'
            - 'false'
      -
        in: query
        name: includeArchived
        description: 'When true, include rows with archivedAt set (governance).'
        schema:
          type: string
          enum:
            - 'true'
            - 'false'
      -
        in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 500
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
'/ai/studio/models/{id}':
  get:
    tags:
      - AI
    summary: 'Get one ML Studio model by id'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
  delete:
    tags:
      - AI
    summary: 'Archive or permanently delete ML Studio model row'
    description: |
      Needs **`ai.refresh`** and **`X-Park-Id`**.
      Default (no query): **soft-archive** — fails with **`MODEL_ACTIVE`** if still active, or **`MODEL_LAST_DEPLOYABLE_VERSION`** if last non-archived row in slot.
      With **`permanent=true`**: **hard-delete** the DB row; only allowed when the row is already archived (`archivedAt` set). Otherwise **`MODEL_NOT_ARCHIVED`** (409).
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      -
        in: query
        name: permanent
        required: false
        description: 'If `true`, permanently remove the row from the database (archived rows only).'
        schema:
          type: boolean
          default: false
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: Not found
      '409':
        description: 'Governance conflict (e.g. MODEL_ACTIVE, MODEL_NOT_ARCHIVED)'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/models/train':
  post:
    tags:
      - AI
    summary: 'Train an ML Studio model (sandbox or feature-store dataset)'
    description: |
      Needs **`ai.refresh`** and **`X-Park-Id`**. Body is validated server-side (`ai-studio.validators.js`).
      **FEATURE_STORE (Phase 2):** supervised training on `ride_feature_snapshots_5m` with optional **`featureStoreTrainingOptions`** (RF / GBM / MLP / ridge λ).
      **SANDBOX:** deterministic stub metrics for algorithm comparison.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiStudioTrainRequest'
    responses:
      '200':
        description: 'OK (or 201 depending on implementation)'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation / business rules'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/models/validate-features':
  post:
    tags:
      - AI
    summary: 'FEATURE_STORE — raw data coverage per X feature (pre-training quality)'
    description: |
      Needs **`ai.read`** and **`X-Park-Id`**. Returns share of eligible snapshot rows (max 8000) where each selected feature has a non-null source before imputation.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - entityType
              - entityId
            properties:
              entityType:
                type: string
                enum:
                  - RIDE
              entityId:
                type: string
                format: uuid
              features:
                type: array
                items:
                  type: string
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation / business rules'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/models/batch-train-status':
  get:
    tags:
      - AI
    summary: 'Phase 3 — batch FEATURE_STORE training progress for current park'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. In-memory status for the last / active park-wide ride batch.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
'/ai/studio/models/batch-train-rides':
  post:
    tags:
      - AI
    summary: 'Start park-wide FEATURE_STORE training for all RIDE assets'
    description: |
      Needs **`ai.refresh`** and **`X-Park-Id`**. Returns **202** immediately; poll **`/ai/studio/models/batch-train-status`** for progress.
    requestBody:
      required: false
      content:
        application/json:
          schema:
            type: object
            properties:
              strategy:
                type: string
                enum:
                  - AUTO
                  - MANUAL
              algorithm:
                type: string
              features:
                type: array
                items:
                  type: string
    responses:
      '202':
        description: Accepted
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '409':
        description: 'Batch already running'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
      '422':
        description: 'Validation / business rules'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/models/batch-apply':
  post:
    tags:
      - AI
    summary: 'Apply last batch algorithms to ride assets and activate FEATURE_STORE models'
    description: |
      Needs **`ai.refresh`** and **`X-Park-Id`**. Body **`batchId`** must match the finished batch from **`/ai/studio/models/batch-train-status`**.
      Updates **`evaluated_algorithm`** on each successful ride row, **activates** the matching **`ai_studio_models`** row for that batch (FEATURE_STORE / `wait_time_plus_15`), deactivating prior active rows in the same slot, clears the ride-forecast resolve cache for the park, then clears in-memory batch state.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - batchId
            properties:
              batchId:
                type: string
                format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Batch not found or batchId mismatch'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
      '409':
        description: 'Batch still running'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
      '422':
        description: 'Nothing to apply / validation'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/models/{id}/activate':
  patch:
    tags:
      - AI
    summary: 'Activate or deactivate one ML Studio model'
    description: 'Needs **`ai.refresh`** and **`X-Park-Id`**. Path **`id`** and body **`activeFlag`** required.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - activeFlag
            properties:
              activeFlag:
                type: boolean
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/models/{id}/restore':
  post:
    tags:
      - AI
    summary: 'Restore archived ML Studio model'
    description: 'Needs **`ai.refresh`** and **`X-Park-Id`**. Clears **`archivedAt`** / **`archivedBy`**.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: Not found
'/ai/studio/predict':
  post:
    tags:
      - AI
    summary: 'ML Studio dry-run prediction from manual features or latest snapshot'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Validated per `predictBody` in `ai-studio.validators.js`.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiStudioPredictRequest'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/studio/runtime-resolution':
  post:
    tags:
      - AI
    summary: 'Explain ML Studio predict model resolution (entity → category → park → fallback)'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Active-flag models only for the final pick; best MAE per level is informational. Validated per `runtimeResolutionBody` in `ai-studio.validators.js`.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiStudioRuntimeResolutionRequest'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
/ai/forecast-accuracy/zone-crowd:
  get:
    tags:
      - AI
    summary: 'Zone crowd forecast accuracy metrics'
    description: 'Needs **`ai.read`**. Query **`days`** (1–90), **`horizonMinutes`** (15, 60, or 180), optional **`limitForecasts`**.'
    parameters:
      -
        in: query
        name: days
        schema:
          type: integer
          minimum: 1
          maximum: 90
          default: 7
      -
        in: query
        name: horizonMinutes
        schema:
          type: integer
          enum:
            - 15
            - 60
            - 180
          default: 60
      -
        in: query
        name: limitForecasts
        schema:
          type: integer
          minimum: 50
          maximum: 800
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/timeseries/rides-current:
  get:
    tags:
      - AI
    summary: 'Current ride wait snapshot series for the active park'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Query **`hours`** (1–168, default 24).'
    parameters:
      -
        in: query
        name: hours
        schema:
          type: integer
          minimum: 1
          maximum: 168
          default: 24
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/training-dataset:
  get:
    tags:
      - AI
    summary: 'Sample rows from the training feature dataset (park-scoped)'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Query **`target`** (`queue_time_15m` \| `queue_time_60m` \| `queue_time_120m`), **`limit`** (1–2000).'
    parameters:
      -
        in: query
        name: target
        schema:
          type: string
          enum:
            - queue_time_15m
            - queue_time_60m
            - queue_time_120m
          default: queue_time_15m
      -
        in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 2000
          default: 400
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
'/ai/timeseries/rides/{assetId}':
  get:
    tags:
      - AI
    summary: 'Ride wait time series for one asset'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Query **`from`** / **`to`** ISO datetimes required; optional **`includeContext`** (boolean).'
    parameters:
      -
        in: path
        name: assetId
        required: true
        schema:
          type: string
          format: uuid
      -
        in: query
        name: from
        required: true
        schema:
          type: string
          format: date-time
      -
        in: query
        name: to
        required: true
        schema:
          type: string
          format: date-time
      -
        in: query
        name: includeContext
        schema:
          type: boolean
          default: true
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
/ai/timeseries/calendar:
  post:
    tags:
      - AI
    summary: 'Upsert per-day park calendar context (holidays, school breaks)'
    description: 'Needs **`ai.refresh`** and **`X-Park-Id`**.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiParkCalendarUpsertBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
/ai/factors/config:
  get:
    tags:
      - AI
    summary: 'AI factor configuration envelope (defaults and metadata)'
    description: 'Needs **`ai.read`**.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/global-factors:
  get:
    tags:
      - AI
    summary: 'List global ML factors'
    description: 'Needs **`ai.read`**.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
  post:
    tags:
      - AI
    summary: 'Create a global ML factor'
    description: 'Needs **`ai.refresh`**.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiGlobalFactorCreateBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/global-factors/{id}':
  patch:
    tags:
      - AI
    summary: 'Patch one global ML factor'
    description: 'Needs **`ai.refresh`**. At least one mutable field required.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiGlobalFactorPatchBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  delete:
    tags:
      - AI
    summary: 'Delete one global ML factor'
    description: 'Needs **`ai.refresh`**.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
'/ai/parks/{parkId}/factors':
  get:
    tags:
      - AI
    summary: 'List park-level factor overrides for ML'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. `parkId` must match park context.'
    parameters:
      -
        in: path
        name: parkId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
  patch:
    tags:
      - AI
    summary: 'Replace park factor weight/value overrides'
    description: 'Needs **`ai.refresh`** and **`X-Park-Id`**. Body requires **`factors`** array.'
    parameters:
      -
        in: path
        name: parkId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiParkFactorsPatchBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
/ai/ml-profiles:
  get:
    tags:
      - AI
    summary: 'List ML profile templates'
    description: 'Needs **`ai.read`**. Optional query **`entityType`**, **`activeFlag`**, **`search`**.'
    parameters:
      -
        in: query
        name: entityType
        schema:
          type: string
          maxLength: 32
      -
        in: query
        name: activeFlag
        schema:
          type: boolean
      -
        in: query
        name: search
        schema:
          type: string
          maxLength: 120
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
  post:
    tags:
      - AI
    summary: 'Create an ML profile template'
    description: 'Needs **`ai.refresh`**. See `mlProfileBody` in `ml-ai.validators.js` for full field set.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiMlProfileCreateBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/ml-profiles/{id}':
  patch:
    tags:
      - AI
    summary: 'Patch one ML profile template'
    description: 'Needs **`ai.refresh`**. At least one field required.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiMlProfilePatchBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  delete:
    tags:
      - AI
    summary: 'Delete one ML profile template'
    description: 'Needs **`ai.refresh`**.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
'/ai/assets/{assetId}/effective-ml-config':
  get:
    tags:
      - AI
    summary: 'Resolved effective ML config for a platform asset'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**.'
    parameters:
      -
        in: path
        name: assetId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Asset not found'
'/ai/assets/{assetId}/ml-overrides':
  patch:
    tags:
      - AI
    summary: 'Patch structured ML override ops for an asset'
    description: 'Needs **`ai.refresh`** and **`X-Park-Id`**. Body requires **`ops`** array.'
    parameters:
      -
        in: path
        name: assetId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiAssetMlOverridesPatchBody'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/assets/{assetId}/ml-profile':
  put:
    tags:
      - AI
    summary: 'Assign ML profile template to an asset'
    description: 'Needs **`ai.refresh`** and **`X-Park-Id`**. Body **`profileId`** (UUID).'
    parameters:
      -
        in: path
        name: assetId
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - profileId
            properties:
              profileId:
                type: string
                format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: 'Not found'
      '422':
        description: 'Validation error'
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
/ai/feature-store/monitor:
  get:
    tags:
      - AI
    summary: 'Feature store freshness / coverage monitor for active park'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/feature-data-quality:
  get:
    tags:
      - AI
    summary: 'Feature completeness / quality dashboard for active park'
    description: 'Needs **`ai.read`** and **`X-Park-Id`**. Optional filters **`from`**, **`to`**, **`entityType`**, **`completenessMax`**, **`missingFeature`**, **`confidenceMax`**, **`provider`**, **`summariesLimit`** (see `featureDataQualityQuery`).'
    parameters:
      -
        in: query
        name: from
        schema:
          type: string
      -
        in: query
        name: to
        schema:
          type: string
      -
        in: query
        name: entityType
        schema:
          type: string
          enum:
            - RIDE
            - SHOW
            - RESTAURANT
            - SHOP
            - ""
      -
        in: query
        name: completenessMax
        schema:
          type: number
      -
        in: query
        name: missingFeature
        schema:
          type: string
          enum:
            - weather
            - calendar
            - traffic
            - staffing
            - ""
      -
        in: query
        name: confidenceMax
        schema:
          type: number
      -
        in: query
        name: provider
        schema:
          type: string
      -
        in: query
        name: summariesLimit
        schema:
          type: integer
          minimum: 10
          maximum: 300
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
/ai/studio/feature-drafts:
  get:
    tags:
      - AI
    summary: 'Get ML Studio dataset feature draft (Phase P)'
    description: 'Needs `ai.read` and active park context (`X-Park-Id`). Returns persisted draft or `data: null`.'
    parameters:
      -
        in: query
        name: entityType
        required: true
        schema:
          type: string
          enum:
            - ride
            - park_asset
      -
        in: query
        name: entityId
        required: true
        schema:
          type: string
          format: uuid
      -
        in: query
        name: datasetScope
        schema:
          type: string
          enum:
            - single_asset
          default: single_asset
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeAiStudioFeatureDraftNullable'
  put:
    tags:
      - AI
    summary: 'Save ML Studio dataset feature draft (Phase P)'
    description: "Needs `ai.refresh` and active park. Each `selectedSignalKey` must exist on ride/asset extensions with `enabled` and `mlEligible`\n(validated via the same **`signal-preview.service`** module as Add-on Board preview gates — **no** `signalPreview` or latest-value fields in this API).\nStored in `app_settings` key `aiStudio.featureDrafts` (JSON), scoped by park.\n"
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AiStudioFeatureDraftPut'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeAiStudioFeatureDraft'
'/ai/parks/{externalParkId}/forecast/summary':
  get:
    tags:
      - AI
    summary: 'Park wait forecast summary (baseline + X + ML enterprise)'
    description: 'Needs `ai.read`. Response `data` includes `mlFactorCurrents` (object, possibly empty).'
    parameters:
      -
        in: path
        name: externalParkId
        required: true
        schema:
          type: string
      -
        in: query
        name: provider
        schema:
          type: string
          example: themeparks_wiki
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeParkForecastSummary'
'/ai/parks/{externalParkId}/forecast/series':
  get:
    tags:
      - AI
    summary: 'Interpolated wait forecast points from latest summary'
    description: 'Needs `ai.read`. Does not return `mlFactorCurrents` (series only).'
    parameters:
      -
        in: path
        name: externalParkId
        required: true
        schema:
          type: string
      -
        in: query
        name: provider
        schema:
          type: string
      -
        in: query
        name: horizon
        schema:
          type: integer
          default: 60
      -
        in: query
        name: step
        schema:
          type: integer
          default: 5
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
'/ai/parks/{externalParkId}/forecast/explanation':
  get:
    tags:
      - AI
    summary: 'Short textual explanation of park forecast delta'
    description: |
      Needs `ai.read`. Does not return `mlFactorCurrents`. Adds `data.explainability` (`AiExplainabilityMvp`, track `ADR_FORECAST`)
      alongside legacy fields. Each `featureContributions[].source` is one of the canonical contribution-source enum values
      (`baseline_integration`, `x_layer_heuristic`, `enterprise_ml`, `snapshot`, `ridge_model`, `baseline_rule`, `ai_studio`, `unknown`).
    parameters:
      -
        in: path
        name: externalParkId
        required: true
        schema:
          type: string
      -
        in: query
        name: provider
        schema:
          type: string
      -
        in: query
        name: horizon
        schema:
          type: integer
          default: 60
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeForecastExplanation'
'/ai/entities/{externalEntityId}/forecast/explanation':
  get:
    tags:
      - AI
    summary: 'Entity (ride) forecast explanation + normalized explainability'
    description: |
      Needs `ai.read`. Same query params as entity forecast summary. Returns legacy explanation fields plus `data.explainability`
      (`AiExplainabilityMvp`). `featureContributions[].source` uses the canonical contribution-source enum (`AiExplainabilityContributionSource`).
    parameters:
      -
        in: path
        name: externalEntityId
        required: true
        schema:
          type: string
      -
        in: query
        name: externalParkId
        required: true
        schema:
          type: string
      -
        in: query
        name: provider
        schema:
          type: string
      -
        in: query
        name: entityType
        schema:
          type: string
      -
        in: query
        name: horizon
        schema:
          type: integer
          default: 60
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeForecastExplanation'
'/ai/entities/{externalEntityId}/forecast/summary':
  get:
    tags:
      - AI
    summary: 'Entity (ride) wait forecast summary'
    description: 'Needs `ai.read`. Query `externalParkId` required. `data` includes `mlFactorCurrents` when merge ran.'
    parameters:
      -
        in: path
        name: externalEntityId
        required: true
        schema:
          type: string
      -
        in: query
        name: externalParkId
        required: true
        schema:
          type: string
      -
        in: query
        name: provider
        schema:
          type: string
      -
        in: query
        name: entityType
        schema:
          type: string
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeParkForecastSummary'
'/ai/parks/{externalParkId}/entities/forecast/summary':
  get:
    tags:
      - AI
    summary: 'Batch entity forecast summaries for a park'
    description: 'Needs `ai.read`. Each item includes `mlFactorCurrents` when merge ran.'
    parameters:
      -
        in: path
        name: externalParkId
        required: true
        schema:
          type: string
      -
        in: query
        name: provider
        schema:
          type: string
      -
        in: query
        name: limit
        schema:
          type: integer
          default: 150
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeParkForecastSummaryList'
'/ai/feature-store/park-snapshots/{id}':
  delete:
    tags:
      - AI
    summary: 'Delete one park feature snapshot row'
    description: 'Needs `ai.refresh` and X-Park-Id; row must belong to that park.'
    parameters:
      -
        $ref: '#/components/parameters/IdPath'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  properties:
                    deleted:
                      type: integer
/ai/feature-store/park-snapshots/purge:
  post:
    tags:
      - AI
    summary: 'Bulk-delete park snapshots in a time window'
    description: 'Needs `ai.refresh` and X-Park-Id; range max 35 days.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - from
              - to
            properties:
              from:
                type: string
                description: ISO8601
              to:
                type: string
                description: ISO8601
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  properties:
                    deleted:
                      type: integer
/ai/feature-store/park-snapshots/bulk-delete:
  post:
    tags:
      - AI
    summary: 'Delete park snapshots by id list'
    description: 'Needs `ai.refresh` and X-Park-Id; max 200 UUIDs; only rows for that park are deleted.'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - ids
            properties:
              ids:
                type: array
                maxItems: 200
                items:
                  type: string
                  format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  properties:
                    deleted:
                      type: integer
                    requested:
                      type: integer
/ai/recommendations/scored:
  get:
    tags:
      - AI
    summary: 'List OPEN recommendations with AI scores (sorted by score)'
    description: 'Needs `ai.read`'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeRecommendationsScored'
/ai/recommendations/scoring-summary:
  get:
    tags:
      - AI
    summary: 'Top risky recommendations and aggregate scoring stats'
    description: 'Needs `ai.read`'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeRecommendationScoringSummary'
/ai/recommendations/score:
  post:
    tags:
      - AI
    summary: 'Score all OPEN recommendations'
    description: 'Needs `ai.refresh` or `recommendations.update`'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeScoreAllResult'
'/ai/recommendations/{id}/score':
  post:
    tags:
      - AI
    summary: 'Score one OPEN recommendation'
    description: 'Needs `ai.refresh` or `recommendations.update`'
    parameters:
      -
        $ref: '#/components/parameters/IdPath'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeScoreOneResult'
'/ai/recommendations/{id}/explanation':
  get:
    tags:
      - AI
    summary: 'Explanation and factors for a scored recommendation'
    description: 'Needs `ai.read`'
    parameters:
      -
        $ref: '#/components/parameters/IdPath'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeRecommendationExplanation'
'/ai/ml/predict/rides/{rideId}':
  get:
    tags:
      - AI
    summary: 'Predict ride wait times (15/30/60 min) from feature store + optional ridge models'
    description: |
      Canonical mount under `/ai/ml/*` (Phase B5). Needs `ai.read` and `X-Park-Id`. Falls back to
      baseline when no model is trained. Identical handler to the deprecated `/ml/predict/rides/{rideId}`.
      By default, response `data` is the legacy prediction payload. Add `?explain=1` (or `?explain=true`)
      to include optional `data.explanation` (`AiExplainabilityMvp`, track `RIDGE_RIDE_WAIT`). Each
      `featureContributions[].source` is an `AiExplainabilityContributionSource` enum value.
    parameters:
      -
        in: path
        name: rideId
        required: true
        schema:
          type: string
          format: uuid
        description: 'Platform ride asset_id (park_assets.asset_id)'
      -
        in: query
        name: horizon
        schema:
          type: string
          example: 1530
        description: 'Comma-separated horizons in minutes'
      -
        in: query
        name: explain
        schema:
          oneOf:
            -
              type: boolean
            -
              type: string
              enum:
                - '1'
                - '0'
                - 'true'
                - 'false'
        description: 'Optional explainability gate. `1`/`true` includes `data.explanation`; omitted or `0`/`false` returns legacy prediction payload only.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlRidePredict'
/ai/ml/predict/park-summary:
  get:
    tags:
      - AI
    summary: 'Add-on Board L0/L1 — aggregate 60m wait forecasts across ride assets'
    description: |
      Canonical mount under `/ai/ml/*` (Phase B5). Needs `ai.read` and `X-Park-Id`. Loads RIDE-type
      `park_assets`, calls the same prediction path as L3 per ride (batched), then rolls up
      `parkDemandForecastIndex`, `averageForecastWaitTime60`, `forecastCriticalRides`, and per-zone
      stats for L1 drill-down. Optional `zoneId` scopes the asset list to one zone (L1 board). Does
      not throw if individual ride predictions fail. Responses are cached about **45 seconds** per
      `(park, zone, threshold, limit, offset)` key.
    parameters:
      -
        in: query
        name: zoneId
        schema:
          type: string
          format: uuid
        description: 'When set, only rides in this zone are included (L1 scope).'
      -
        in: query
        name: criticalAtMinutes
        schema:
          type: number
          minimum: 15
          maximum: 180
          default: 55
        description: 'Count rides at or above this forecasted wait (60 min horizon) as critical.'
      -
        in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 80
        description: 'Max ride assets to evaluate (caps load).'
      -
        in: query
        name: offset
        schema:
          type: integer
          minimum: 0
          maximum: 500
          default: 0
        description: 'Skip this many ride assets (stable name order) for paging large parks.'
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlParkForecastSummary'
/ai/ml/dataset/rides:
  get:
    tags:
      - AI
    summary: 'ML dataset stats from ride_feature_snapshots_5m (join coverage)'
    description: |
      Canonical mount under `/ai/ml/*` (Phase B5). Needs `ai.read` and park context.
    parameters:
      -
        in: query
        name: rideId
        schema:
          type: string
          format: uuid
      -
        in: query
        name: from
        schema:
          type: string
          format: date-time
      -
        in: query
        name: to
        schema:
          type: string
          format: date-time
      -
        in: query
        name: rowLimit
        schema:
          type: integer
          minimum: 100
          maximum: 50000
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlRideDataset'
/ai/ml/train/wait-time/global:
  post:
    tags:
      - AI
    summary: 'Train global ridge wait-time models (15/30/60) for current park'
    description: |
      Canonical mount under `/ai/ml/*` (Phase B5). Needs `ai.refresh` and `X-Park-Id`.
    requestBody:
      required: false
      content:
        application/json:
          schema:
            type: object
            properties:
              rowLimit:
                type: integer
                minimum: 200
                maximum: 50000
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlTrainResult'
'/ai/ml/train/wait-time/rides/{rideId}':
  post:
    tags:
      - AI
    summary: 'Train ride-specific models when data criteria are met'
    description: |
      Canonical mount under `/ai/ml/*` (Phase B5). Needs `ai.refresh` and `X-Park-Id`.
    parameters:
      -
        in: path
        name: rideId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlTrainRideResult'
'/ai/ml/prediction-traces':
  get:
    tags:
      - AI
    summary: 'List ML prediction traces for the current park (Phase 1 observability)'
    description: |
      **Read-only.** Governed feature snapshots only; requires `ai.read` and `X-Park-Id`. Results are limited to the
      authenticated park. Populated only when `ML_TRACE_ENABLED=true` on the API process; does not affect predictions.
    parameters:
      -
        in: query
        name: rideId
        schema:
          type: string
          format: uuid
        description: 'Filter by ride asset_id'
      -
        in: query
        name: modelName
        schema:
          type: string
      -
        in: query
        name: targetName
        schema:
          type: string
      -
        in: query
        name: from
        schema:
          type: string
          format: date-time
      -
        in: query
        name: to
        schema:
          type: string
          format: date-time
      -
        in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 50
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlPredictionTraces'
'/ai/ml/prediction-traces/filter-options':
  get:
    tags:
      - AI
    summary: 'Distinct model_name and target_name values for trace list filters (current park)'
    description: |
      **Read-only.** Alphabetically sorted distinct values from `ml_prediction_traces` for the authenticated park
      (`X-Park-Id`). Omits null/empty names. Does not depend on `ML_TRACE_ENABLED` for reads (returns empty arrays when
      no rows exist).
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlPredictionTraceFilterOptions'
'/ai/ml/prediction-traces/{predictionId}/coefficients':
  get:
    tags:
      - AI
    summary: 'Learned ridge coefficients + manual weights for a prediction trace (read-only)'
    description: |
      **Read-only.** Resolves the trace’s model registry row and projects coefficient rows when present; requires `ai.read` and `X-Park-Id`.
    parameters:
      -
        in: path
        name: predictionId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
'/ai/ml/prediction-traces/{predictionId}':
  get:
    tags:
      - AI
    summary: 'Get one prediction trace and per-horizon results by prediction_id UUID'
    description: |
      **Read-only.** Includes `trace` and `results` (per horizon). Requires `ai.read` and `X-Park-Id`; 404 if missing or not in current park.
    parameters:
      -
        in: path
        name: predictionId
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeMlPredictionTraceDetail'
      '404':
        description: Not found
'/ai/ml/forecast-accuracy/kpis':
  get:
    tags:
      - AI
    summary: 'Forecast accuracy KPIs (observational)'
    description: |
      **Read-only.** Aggregates `ml_forecast_accuracy_logs` for the current park; compares past `ml_prediction_results`
      to governed `ride_feature_snapshots_5m` at prediction time + horizon. Does not alter forecasts or training.
    parameters:
      -
        in: query
        name: rideId
        schema:
          type: string
          format: uuid
      -
        in: query
        name: modelName
        schema:
          type: string
      -
        in: query
        name: targetName
        schema:
          type: string
      -
        in: query
        name: horizonMinutes
        schema:
          type: integer
          minimum: 0
          maximum: 1440
      -
        in: query
        name: from
        schema:
          type: string
          format: date-time
      -
        in: query
        name: to
        schema:
          type: string
          format: date-time
      -
        in: query
        name: comparableOnly
        schema:
          type: boolean
        description: 'When true, restrict aggregates to rows with a usable actual (OK/WARNING/CRITICAL only).'
    responses:
      '200':
        description: OK
'/ai/ml/forecast-accuracy/model-win-rates':
  get:
    tags:
      - AI
    summary: 'Forecast accuracy — model win-rate stats (observational)'
    description: |
      **Read-only.** Compares which ridge model names win head-to-head for comparable rows in `ml_forecast_accuracy_logs` (current park).
      Requires `ai.read` and `X-Park-Id`.
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
'/ai/ml/forecast-accuracy/retro-lookback':
  get:
    tags:
      - AI
    summary: 'Forecast accuracy — retro lookback window summary'
    description: |
      **Read-only.** Returns bounded historical slices for accuracy dashboards; requires `ai.read` and `X-Park-Id`.
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
'/ai/ml/forecast-accuracy/{id}':
  get:
    tags:
      - AI
    summary: 'Get one forecast accuracy log row'
    description: '**Read-only.** Scoped to current park (`X-Park-Id`).'
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
      '404':
        description: Not found
'/ai/ml/forecast-accuracy':
  get:
    tags:
      - AI
    summary: 'List forecast accuracy evaluation logs'
    description: |
      **Read-only.** Observational accuracy rows derived from prediction traces and governed snapshots only.
    parameters:
      -
        in: query
        name: rideId
        schema:
          type: string
          format: uuid
      -
        in: query
        name: modelName
        schema:
          type: string
      -
        in: query
        name: targetName
        schema:
          type: string
      -
        in: query
        name: horizonMinutes
        schema:
          type: integer
          minimum: 0
          maximum: 1440
      -
        in: query
        name: from
        schema:
          type: string
          format: date-time
      -
        in: query
        name: to
        schema:
          type: string
          format: date-time
      -
        in: query
        name: comparableOnly
        schema:
          type: boolean
        description: 'When true, only list rows that have comparable predicted vs actual (excludes UNKNOWN).'
      -
        in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 500
    responses:
      '200':
        description: OK
'/ai/ml/feature-store-readiness':
  get:
    tags:
      - AI
    summary: 'Feature store readiness (ride_feature_snapshots_5m)'
    description: |
      **Read-only.** Aggregates AI sampling / pipeline health and ride snapshot aggregates for `X-Park-Id`.
      Optional `from` / `to` (ISO-8601) default to the trailing 7 days; optional `rideId` scopes to one ride.
      Does **not** mutate forecasts or snapshots.
    parameters:
      - in: query
        name: rideId
        schema:
          type: string
          format: uuid
      - in: query
        name: from
        schema:
          type: string
          format: date-time
      - in: query
        name: to
        schema:
          type: string
          format: date-time
    responses:
      '200':
        description: OK
      '422':
        description: Invalid date range
'/ai/ml/feature-store-snapshot-debug':
  get:
    tags:
      - AI
    summary: 'Ride snapshot SQL-style debug (counts + latest rows)'
    description: |
      **Read-only.** For the active park and **required** `rideId`, returns rolling-window counts on `ride_feature_snapshots_5m`
      and the latest snapshot rows (bounded). Does not change data.
    parameters:
      - in: query
        name: rideId
        required: true
        schema:
          type: string
          format: uuid
      - in: query
        name: windowHours
        schema:
          type: integer
          minimum: 1
          maximum: 72
          default: 2
    responses:
      '200':
        description: OK
      '422':
        description: Missing or invalid rideId / window
'/ai/ml/park-profiles':
  get:
    tags:
      - AI
    summary: 'List ML park metadata profiles (ML_PROFILE_ENABLED)'
    description: |
      Requires `ai.read`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`. Query validated (`parkListQuery`).
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '403':
        description: Forbidden
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  post:
    tags:
      - AI
    summary: 'Create ML park metadata profile'
    description: Requires `ai.refresh`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`. Body per `parkProfileBody`.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '403':
        description: Forbidden
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/ml/park-profiles/{id}':
  get:
    tags:
      - AI
    summary: 'Get one ML park metadata profile'
    description: Requires `ai.read`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`.
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
  put:
    tags:
      - AI
    summary: 'Replace ML park metadata profile'
    description: Requires `ai.refresh`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`. Body per `parkProfilePutMerged`.
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  delete:
    tags:
      - AI
    summary: 'Archive ML park metadata profile (soft delete)'
    description: 'Requires `ai.refresh`, `X-Park-Id`, ML_PROFILE_ENABLED. Profile must have **`enabled=false`** first (`PROFILE_ENABLED` otherwise).'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
      '409':
        description: Conflict
'/ai/ml/ride-profiles':
  get:
    tags:
      - AI
    summary: 'List ML ride metadata profiles (ML_PROFILE_ENABLED)'
    description: |
      Requires `ai.read`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`. Query validated (`rideListQuery`).
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '403':
        description: Forbidden
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  post:
    tags:
      - AI
    summary: 'Create ML ride metadata profile'
    description: Requires `ai.refresh`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`. Body per `rideProfileBody`.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '403':
        description: Forbidden
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
'/ai/ml/ride-profiles/{id}':
  get:
    tags:
      - AI
    summary: 'Get one ML ride metadata profile'
    description: Requires `ai.read`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`.
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  put:
    tags:
      - AI
    summary: 'Replace ML ride metadata profile'
    description: Requires `ai.refresh`, `X-Park-Id`, and `ML_PROFILE_ENABLED=true`. Body per `rideProfilePutMerged`.
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
  delete:
    tags:
      - AI
    summary: 'Archive ML ride metadata profile (soft delete)'
    description: 'Same governance as park profiles — **`enabled=false`** required before archive.'
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '404':
        description: Not found
      '409':
        description: Conflict
'/ai/ml/registry/entries/{id}':
  delete:
    tags:
      - AI
    summary: 'Archive or permanently delete ML model registry ridge row'
    description: |
      Requires **`ai.refresh`**. Default: **soft-archive** — excludes row from ride **`findActiveModel`** resolution. Fails if **`MODEL_ACTIVE`** or **`MODEL_LAST_DEPLOYABLE_VERSION`**.
      With **`permanent=true`**: hard-delete from DB (**archived rows only**); otherwise **`MODEL_NOT_ARCHIVED`**.
    parameters:
      -
        in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      -
        in: query
        name: permanent
        required: false
        description: 'If `true`, permanently remove the row (archived rows only).'
        schema:
          type: boolean
          default: false
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvelopeGenericData'
      '401':
        description: Unauthorized
      '403':
        description: Forbidden
      '404':
        description: Not found
      '409':
        description: Governance conflict
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorBody'
